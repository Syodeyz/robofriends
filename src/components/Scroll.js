import React from 'react';

const Scroll = (props) => {
    return (
        <div className=" b--solid bw3 b--lightest-blue">
            {props.children}
        </div>
        
    );
}
export default Scroll;